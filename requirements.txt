# ============================================================
# requirements.txt
# Metric Reliability in Abstractive Summarization
# Varun D Kanchan & Abhishek Shetty
# Manipal Institute of Technology, 2025
# ============================================================
# Python 3.10 REQUIRED
# Install: pip install -r requirements.txt
# ============================================================

# ── Core PyTorch ────────────────────────────────────────────
torch>=2.0.0                   # GPU strongly recommended
torchvision>=0.15.0

# ── Transformers & Datasets ─────────────────────────────────
transformers==4.38.2           # PINNED — required by bert-score 0.3.13
datasets>=2.18.0
huggingface-hub>=0.20.0
tokenizers>=0.15.0
accelerate>=0.27.0

# ── Lexical Metrics ─────────────────────────────────────────
sacrebleu>=2.3.1               # BLEU + chrF++
rouge-score>=0.1.3             # ROUGE-1/2/L
nltk>=3.8.1                    # METEOR, tokenization

# ── Embedding-Based Metrics ─────────────────────────────────
bert-score==0.3.13             # PINNED — Python 3.10 compatible
sentence-transformers>=2.6.1  # MoverScore Hungarian proxy

# ── Learned / Neural Metrics ────────────────────────────────
unbabel-comet>=2.2.1           # COMET (wmt20-comet-da)
# bleurt: install from source — see README.md

# ── Scientific Computing ─────────────────────────────────────
numpy>=1.24.0
scipy>=1.11.0
pandas>=2.0.0
scikit-learn>=1.3.0

# ── Visualization ───────────────────────────────────────────
matplotlib>=3.7.0
seaborn>=0.12.0

# ── Output & Storage ────────────────────────────────────────
openpyxl>=3.1.0                # Excel workbook output
tqdm>=4.66.0

# ── Reproducibility & Utilities ─────────────────────────────
python-dotenv>=1.0.0
packaging>=23.0
